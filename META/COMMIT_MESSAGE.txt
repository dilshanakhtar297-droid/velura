Updated: moved API routes into Next.js API routes for single-Vercel deployment (serverless functions). Converted auth, products, upload presign/complete, Stripe webhook/create-session, and orders endpoints into web/pages/api/*. The api/ directory is preserved as a backup but the Next.js app now contains the runtime API suitable for Vercel.
